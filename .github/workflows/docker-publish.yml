name: Migrate v1.5.4 into source

on:
  push:
<<<<<<< HEAD
    branches: ["main"]
=======
    branches: [main]
>>>>>>> 945a58d (Release v1.5.4)
  workflow_dispatch:

permissions:
  contents: write

jobs:
<<<<<<< HEAD
  migrate:
=======
  test-and-publish:
>>>>>>> 945a58d (Release v1.5.4)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
<<<<<<< HEAD
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply optimized matcher directly
        shell: bash
        run: |
          set -euo pipefail
          git apply --check release/v1.5.4.patch
          git apply release/v1.5.4.patch

      - name: Add manual MAL ID assignment directly
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          p = Path('main.go')
          s = p.read_text()

          route = '\tmux.HandleFunc("/api/cache/delete", app.deleteCacheEntryAPI)\n'
          manual_route = route + '\tmux.HandleFunc("/api/cache/manual", app.manualCacheEntryAPI)\n'
          if '/api/cache/manual' not in s:
              s = s.replace(route, manual_route, 1)

          anchor = '<div class="card"><h2>Sincronización</h2>'
          card = '<div class="card"><h2>Asignación manual de MAL</h2><p class="muted">Úsalo cuando el matching automático falle. El ID de AnimeAV1 aparece en los resultados. Puedes indicar una segunda ficha MAL para temporadas divididas.</p><form method="post" action="/api/cache/manual"><label>ID de AnimeAV1</label><input type="number" min="1" required name="media_id" placeholder="Ej.: 2674"><label>ID de MyAnimeList</label><input type="number" min="1" required name="mal_id" placeholder="Ej.: 25131"><label>Segundo ID de MyAnimeList (opcional)</label><input type="number" min="1" name="mal_id_2" placeholder="Solo para temporadas partidas"><button>Guardar coincidencia manual</button></form></div>\n'
          if 'Asignación manual de MAL' not in s:
              s = s.replace(anchor, card + anchor, 1)

          handler = r'''

func (a *App) manualCacheEntryAPI(w http.ResponseWriter, r *http.Request) {
	if r.Method != http.MethodPost {
		http.Error(w, "POST", http.StatusMethodNotAllowed)
		return
	}
	a.mu.Lock()
	running := a.running
	cookie := a.state.Settings.Cookie
	a.mu.Unlock()
	if running {
		http.Error(w, "detén la sincronización antes de modificar la caché", http.StatusConflict)
		return
	}
	if err := r.ParseForm(); err != nil {
		http.Error(w, "formulario no válido", http.StatusBadRequest)
		return
	}
	mediaID, err1 := strconv.Atoi(strings.TrimSpace(r.FormValue("media_id")))
	malID, err2 := strconv.Atoi(strings.TrimSpace(r.FormValue("mal_id")))
	malID2 := 0
	if raw := strings.TrimSpace(r.FormValue("mal_id_2")); raw != "" {
		var err error
		malID2, err = strconv.Atoi(raw)
		if err != nil || malID2 <= 0 {
			http.Error(w, "mal_id_2 no válido", http.StatusBadRequest)
			return
		}
	}
	if err1 != nil || mediaID <= 0 || err2 != nil || malID <= 0 {
		http.Error(w, "media_id o mal_id no válido", http.StatusBadRequest)
		return
	}

	items, err := a.scrapeContext(r.Context(), cookie)
	if err != nil {
		http.Error(w, "no se pudo consultar AnimeAV1: "+err.Error(), http.StatusBadGateway)
		return
	}
	var source *AVItem
	for i := range items {
		if items[i].MediaID == mediaID {
			source = &items[i]
			break
		}
	}
	if source == nil {
		http.Error(w, "media_id no encontrado en la biblioteca de AnimeAV1", http.StatusNotFound)
		return
	}

	fields := "id,title,alternative_titles,num_episodes,media_type,start_date,my_list_status"
	var anime MALAnime
	if err := a.malRequestContext(r.Context(), http.MethodGet, fmt.Sprintf("/anime/%d?fields=%s", malID, fields), nil, &anime); err != nil {
		http.Error(w, "ID MAL principal no válido: "+err.Error(), http.StatusBadRequest)
		return
	}
	var anime2 MALAnime
	if malID2 > 0 {
		if err := a.malRequestContext(r.Context(), http.MethodGet, fmt.Sprintf("/anime/%d?fields=%s", malID2, fields), nil, &anime2); err != nil {
			http.Error(w, "segundo ID MAL no válido: "+err.Error(), http.StatusBadRequest)
			return
		}
	}

	seen, status := animeState(anime)
	entry := CacheEntry{
		MediaID: mediaID, MALID: anime.ID, MALTitle: anime.Title,
		SourceTitle: source.Title, SourceSeen: source.Seen, SourceStatus: source.Status, SourceTotal: source.Total,
		MALSeen: seen, MALStatus: status, MatchScore: 999, MatchType: "manual",
		LastValidated: time.Now().Unix(), UpdatedAt: time.Now().Unix(), MatcherVersion: appVersion,
		SearchStrategy: "manual_id",
	}
	if anime2.ID > 0 {
		seen2, status2 := animeState(anime2)
		entry.MALID2, entry.MALTitle2 = anime2.ID, anime2.Title
		entry.MAL2Episodes, entry.MAL2Seen, entry.MAL2Status = anime2.NumEpisodes, seen2, status2
	}
	a.cachePut(entry)
	a.appendHistory(map[string]any{"ts": time.Now().Unix(), "event": "manual_match_saved", "media_id": mediaID, "source_title": source.Title, "mal_id": anime.ID, "mal_title": anime.Title, "mal_id_2": anime2.ID, "mal_title_2": anime2.Title})
	redirectHome(w, r)
}
'''
          if 'func (a *App) manualCacheEntryAPI' not in s:
              s += handler

          p.write_text(s)
          PY

      - name: Remove release patches and install clean workflow
        shell: bash
        run: |
          rm -f release/v1.5.4.patch release/v1.5.4-manual-mal-id.patch
          rmdir release 2>/dev/null || true
          cat > .github/workflows/docker-publish.yml <<'YAML'
          name: Test and publish Docker image

          on:
            push:
              branches: ["main"]
            workflow_dispatch:

          permissions:
            contents: read

          concurrency:
            group: docker-publish-main
            cancel-in-progress: true

          jobs:
            test-and-publish:
              runs-on: ubuntu-latest
              steps:
                - uses: actions/checkout@v4

                - name: Read version
                  id: version
                  shell: bash
                  run: |
                    VERSION="$(tr -d '[:space:]' < VERSION)"
                    test -n "$VERSION"
                    echo "version=$VERSION" >> "$GITHUB_OUTPUT"

                - uses: actions/setup-go@v5
                  with:
                    go-version-file: go.mod

                - name: Run tests
                  run: go test ./...

                - uses: docker/setup-qemu-action@v3
                - uses: docker/setup-buildx-action@v3
                - uses: docker/login-action@v3
                  with:
                    username: ${{ secrets.DOCKERHUB_USERNAME }}
                    password: ${{ secrets.DOCKERHUB_TOKEN }}

                - name: Build and publish ARMv7 image
                  uses: docker/build-push-action@v6
                  with:
                    context: .
                    platforms: linux/arm/v7
                    push: true
                    tags: |
                      ovelayos/animeav1-mal-sync:${{ steps.version.outputs.version }}
                      ovelayos/animeav1-mal-sync:latest
                    labels: |
                      org.opencontainers.image.title=animeav1-mal-sync
                      org.opencontainers.image.version=${{ steps.version.outputs.version }}
                      org.opencontainers.image.revision=${{ github.sha }}
          YAML

      - name: Commit migrated source
        shell: bash
        run: |
          set -euo pipefail
          gofmt -w main.go
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add main.go .github/workflows/docker-publish.yml release VERSION docker-compose.portainer.yml 2>/dev/null || git add -A
          git commit -m "v1.5.4: integrate matcher optimizations and manual MAL IDs"
          git push
=======
          go-version-file: go.mod
      - run: go test ./...
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - id: version
        shell: bash
        run: echo "value=$(tr -d '[:space:]' < VERSION)" >> "$GITHUB_OUTPUT"
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/arm/v7
          push: true
          tags: |
            ovelayos/animeav1-mal-sync:${{ steps.version.outputs.value }}
            ovelayos/animeav1-mal-sync:latest
>>>>>>> 945a58d (Release v1.5.4)
